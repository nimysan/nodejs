block nav
	nav(class='navbar navbar-default', role="navigation")
		<div id='announcement'><span class="glyphicon glyphicon glyphicon-volume-up" aria-hidden="true"></span> 【公告】2014年8月1日起，雅潼照片服务公司正式成立了! </div>
		div.container
			div.container-fluid
				div.navbar-header
					a.navbar-brand
						雅潼画社
						
				div(class="collapse navbar-collapse")
					ul(class="nav navbar-nav")
						li.active(role="presentation") <a href="/"><span class="glyphicon glyphicon glyphicon glyphicon-home" aria-hidden="true"></span>主页</a>
						li(role="presentation") <a href="/price">产品价格</a>
					
					form(class="navbar-form navbar-left", role="search")
						div.form-group
							input(type="text",class="form-control",placeholder="输入风格 如 海边")
						button(type="submit", class="btn btn-primary") 搜索
					
					ul(class="nav navbar-nav navbar-right")
						- if (user && (user.displayName || user.loginId))
							- var name = user.displayName || user.loginId;
							li.active
								- if (user.roles && user.roles.length > 0 && user.roles[0] == '54917369df4773fbf217e7cf')
									a(href="/admin/user")= name
								- else
									a(href="/user/#{user.loginId}/index")= name
								li.active <a href="/user/logout">退出</a>
						- else
							li.active <a href="/user/login">登录</a>
							